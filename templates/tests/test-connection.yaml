{{/*
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "robotshop.fullname" . }}-test-connection"
  labels:
    {{- include "robotshop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "robotshop.fullname" . }}-fe:{{ .Values.fe_service.port }}']
  restartPolicy: Never
*/}}